# Скопируйте в `.env` и заполните значения.
# Проект: vh20xg14 / dataset production (заданы в sanity.config.ts).

# Токен с правами Editor (create/update): sanity.io/manage → проект → API → Tokens
SANITY_API_TOKEN=
